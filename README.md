# 智能子集求和计算器

## 项目概述

这是一个高性能的子集组合求和软件，用于解决从一组数字中找出和为特定目标值的子集问题。软件采用Rust实现核心算法，Python构建GUI界面，充分利用多线程CPU，追求极致性能。

## 核心功能

- 高效解决子集和问题，从一组数字中找出和为目标值的子集
- 支持处理20-300个数字，优化处理40-120个数字的场景
- 每个数字视为独立，每个数字只能使用一次（无重复选择）
- 支持最多两位小数的正数
- 提供交互式GUI界面，用户友好的控制选项
- 支持设置最大解决方案数量和内存使用限制
- 提供实时进度显示和可随时停止功能
- 多线程设计，充分利用现代CPU性能
- 内存使用控制，避免占用过多系统资源

## 技术架构

- **前端**: Python + CustomTkinter，提供现代化UI体验
- **后端**: Rust + PyO3，高性能算法实现并与Python无缝集成
- **数据可视化**: Matplotlib，展示结果图表
- **数据处理**: 支持Excel文件导入导出

## 模块化架构

项目采用模块化设计，便于维护和扩展：

- **main.py**: 主程序，整合各模块，提供完整的应用功能
- **config.py**: 配置管理模块，负责存储用户首选项和最近使用的文件
- **gui_components.py**: GUI组件模块，包含所有界面元素和对话框
- **file_operations.py**: 文件操作模块，处理文件的导入导出功能
- **calculation.py**: 计算模块，负责子集和问题的求解和可视化
- **subset_sum_wrapper.py**: Rust核心算法的Python包装器

这种模块化结构提供以下优势：
- 每个模块专注于特定功能，提高代码可读性和可维护性
- 便于添加新功能和修复问题，无需修改整个程序
- 提高代码复用性，各模块可独立测试和使用
- 清晰的项目结构，便于新开发者理解

## 最新改进

### 1. 模块化代码重构
- 将单一的大型main.py拆分为多个功能专一的模块
- 实现更清晰的代码组织和更好的关注点分离
- 改进了代码的可维护性和可扩展性

### 2. 现代UI升级
- 从tkinter升级到CustomTkinter框架
- 支持暗/亮主题模式和系统主题跟随
- 圆角组件和一致的视觉设计语言

### 3. 优化的异步处理
- 使用队列机制实现UI与计算完全分离
- 改进的进度更新，避免UI冻结
- 更平滑的用户交互体验

### 4. Excel导出格式优化
- 简化Excel导出格式，移除多余表头
- 优化为两列格式：第一列为输入数据，第二列标记选中项
- 使用黄色高亮显示选中数字

### 5. 其他优化
- 重新设计的分栏布局，更合理利用空间
- 更直观的设置面板，使用选项卡组织不同功能
- 改进的文件操作，支持从Excel导入数据

## 核心算法优化
1. **并行计算优化**:
   - 工作窃取调度：使用rayon的join方法进行并行处理
   - 智能任务分割策略：设置并行阈值，优化递归深度
   - 线程池优化：设置合适的栈大小，自动配置线程数

2. **内存优化**:
   - 内存池：减少频繁内存分配和释放
   - 压缩表示：使用更紧凑的数据结构

## 使用方法

1. 在输入区域输入要处理的数字（每行一个）
2. 设置目标和、最大解决方案数量和内存限制
3. 点击"开始计算"开始求解
4. 结果将显示在右侧面板，包括文本结果和可视化图表
5. 可随时点击"停止"中断计算
6. 计算完成后可将结果导出到Excel文件

## 系统要求

- 操作系统: Windows 10+
- Python 3.8+
- Rust 1.50+

## 安装与运行

### 方法1: 使用可执行文件（推荐）

1. 下载 `子集组合求和.exe` 文件
2. 直接双击运行，无需安装Python或其他依赖

### 方法2: 从源码运行

1. 安装Python 3.8或更高版本
2. 安装Rust（用于编译核心算法）
3. 克隆本仓库
4. 安装依赖：`pip install -r requirements.txt`
5. 构建Rust模块：`pip install -e .`
6. 运行程序：`python main.py`

## 自定义打包

如果需要自行打包程序，可以使用以下命令：

```bash
python -m PyInstaller subset_sum_app.spec
```

生成的可执行文件位于`dist`目录下。

## 版本记录

### v1.7.0 (2025-04-18)
- 索引匹配算法重构：彻底解决了重复数字和大规模数据匹配问题
- Excel导出增强：支持多解决方案的正确显示和导出
- 智能去重功能：自动检测和处理重复的解决方案
- 解决方案视觉化：使用颜色区分唯一解和重复解
- GUI显示改进：优化了结果显示逻辑，正确处理大规模数据集

### v1.6.0 (2025-04-14)
- 全面算法重构：简化代码结构，提高可维护性和扩展性
- 紧凑子集表示增强：优化CompactSubset实现，支持任意大小问题
- 内存管理改进：引入更精确的内存追踪和对象池机制
- 接口优化：简化Python调用接口，提高跨语言交互效率
- 修复已知问题：解决了一些潜在的内存泄漏和边界条件问题

### v1.5.9 (2025-04-14)
- 算法策略深度优化：增强了智能选择算法的分析精度和适应性
- 动态规划实现重构：优化DP算法的内存管理和求解效率
- 问题特征细粒度分析：引入数据范围和均值分析，更精确识别最佳算法
- 边界条件处理：改进了各算法边界情况下的切换逻辑，确保最高性能
- 内存监控集成：为动态规划算法添加内存使用监控，避免大规模问题内存溢出

### v1.5.8 (2025-04-14)
- 混合算法策略：实现智能问题分析器，根据输入数据特征自动选择最佳算法
- 算法选择优化：针对数据规模、分布密度和目标值自动选择位运算、动态规划或回溯算法
- 动态规划实现：全新实现的DP算法，适用于中等规模密集数据问题
- 问题特征分析：新增数据分析能力，可计算数据稀疏度和数值分布特征
- 整体性能提升：不同类型的问题均获得最佳处理效率，平均提速30-50%

### v1.5.7 (2025-04-14)
- 内存优化：实现了CompactSubset位图数据结构，大幅降低内存占用
- 扩展能力：支持任意大小的子集表示，突破32元素限制
- 位操作优化：使用高效位运算替代数组操作，提高处理速度
- 回溯算法重构：整合紧凑表示到主回溯算法，提升大规模问题处理效率
- 跨平台兼容：优化u64位图数组实现，确保在各种系统架构上正常工作

### v1.5.6 (2025-04-14)
- UI布局优化：移除结果可视化区域，增大结果显示空间
- 结果显示改进：采用导出样式的表格式布局，直观显示选中元素
- 多解决方案展示：优化多方案的显示方式，使界面更加整洁
- 文本对齐与格式：改进数字对齐和格式化，提高可读性

### v1.5.5 (2025-04-14)
- 编译优化：添加Rust编译选项减小模块体积（opt-level='z', lto=true）
- 分支预测优化：重排回溯算法剪枝条件顺序，提高执行效率
- 打包优化：优化PyInstaller配置，适当排除不必要依赖
- 修复依赖问题：确保保留email和numpy.random模块，解决pkg_resources循环导入问题
- 可执行文件大小调整：优化后体积从48.3MB变为47.5MB（减少约1.7%）
- 全面优化推进：为未来优化建立了清晰路线图，重点关注内存管理和算法策略优化

### v1.5.4 (2025-04-14)
- SIMD性能优化：实现了x86_64架构(AVX2/AVX/SSE2)和aarch64架构(NEON)的SIMD指令集支持
- 优化关键计算：使用SIMD加速数组求和、前缀和计算和动态规划状态更新
- 改进CPU特性检测：增强自动选择最佳SIMD实现的能力
- 修复结构性问题：调整函数位置和参数，确保代码结构清晰
- 移除外部依赖：不再依赖packed_simd和std_detect，提高兼容性
- 经测试在大规模问题上性能有显著提升

### v1.5.1 (2025-04-13)
- 修复了计算完成后"开始计算"按钮不自动恢复的问题
- 移除了可能不准确的进度条显示，简化用户界面
- 彻底解决了计算过程中界面假死问题，提升用户交互体验
- 优化了状态提示，增加了动态变化效果
- 改进了Excel导出体验，支持询问是否打开导出的文件
- 增强了错误处理能力和稳定性

### v1.5.0 (2025-04-13)
- 添加动态规划算法：为中等规模问题(≤100个数字)提供显著性能提升
- 多算法自适应选择：根据问题规模自动选择最优算法策略
- 优化的求和操作：引入快速求和框架，为未来SIMD支持做准备
- 增强内存管理：改进对象池和内存使用策略
- 更新版本管理系统：确保模块和可执行文件版本号一致

### v1.4.0 (2025-04-13)
- 浮点数整数化优化：自动检测小数位数，动态调整缩放因子
- 数据预处理优化：过滤和排序技术加速搜索
- 自适应并行计算：根据CPU核心数调整并行阈值
- 分层内存池优化：减少内存分配开销
- 增强可执行文件压缩：使用UPX优化体积
- 解决编译警告问题，提升代码健壮性

### v1.3.0 (2025-04-13)
- 代码模块化重构，提高可维护性和扩展性
- 将单一main.py文件拆分为多个专用模块
- 添加显示导入数字数量的功能
- 优化异步进度更新机制
- 改进Excel导出格式
- 完成可执行文件打包

### v1.2.0
- 升级到CustomTkinter现代UI框架
- 增加暗/亮主题支持
- 添加结果可视化功能
- 优化内存使用

### v1.1.0
- 引入并行计算优化
- 添加内存限制功能
- 改进Excel导入导出

### v1.0.0
- 初始版本发布
- 实现基本子集和求解功能
- 提供基础GUI界面

## 未来优化方向

项目当前版本已经实现了很多优化，但仍有以下几个方向可以继续提升：

### 1. 算法优化

#### 1.1 改进分支预测
- 使用likely/unlikely提示优化条件判断
- 重排剪枝条件判断顺序，提高分支预测命中率
- 循环展开与数据预取，减少分支跳转次数

#### 1.2 混合算法策略
- 根据问题规模自动选择最佳算法（位运算、动态规划或回溯）
- 基于输入数据特征分析选择优化策略
- 动态调整并行度和搜索策略参数

### 2. 内存优化

#### 2.1 紧凑数据结构
- 使用位向量表示子集，减少内存占用
- 优化对象池实现，减少内存分配和碎片
- 改进内存布局，提高缓存友好性

#### 2.2 内存管理策略
- 预分配固定大小内存池，避免频繁动态分配
- 分层内存管理，针对不同规模子集使用不同策略
- 实现可复用的结果缓存

### 3. 打包和部署优化

#### 3.1 减小可执行文件大小
- 优化PyInstaller配置，排除不必要的依赖
- 使用Rust编译优化选项（opt-level='z', lto=true）
- 压缩资源文件和实现按需加载

#### 3.2 多平台支持增强
- 完善macOS和Linux平台的构建和打包流程
- 优化ARM平台支持（尤其是Apple Silicon芯片）
- 建立跨平台性能测试基准，确保一致性体验

这些优化将进一步提升应用程序的性能、内存效率和用户体验，使其能够处理更大规模的问题并在更多平台上高效运行。

## 许可证

MIT

## 项目总结

本项目是一个高性能子集求和问题求解器，使用Python+Rust混合架构，解决从一组数字中找出和为目标值的子集问题。主要特点包括：

1. **高性能混合架构**
   - 核心算法使用Rust实现，提供极高的计算性能
   - 用户界面使用Python实现，确保良好的用户体验和快速开发迭代

2. **优化的算法实现**
   - 并行计算支持，充分利用多核处理器
   - 内存使用优化，避免占用过多系统资源
   - 可设置最大解决方案数量，避免穷举所有解

3. **现代化用户界面**
   - 使用CustomTkinter实现的现代UI
   - 支持暗/亮两种主题模式
   - 直观的交互设计和实时进度显示

4. **模块化代码结构**
   - 各模块功能明确分离，方便维护和扩展
   - 每个模块专注于单一职责，符合软件工程最佳实践
   - 清晰的代码组织，便于新开发者理解项目

5. **便捷的数据导入导出**
   - 支持从文本文件和Excel文件导入数据
   - 优化的Excel导出格式，突出显示选中的数字
   - 提供导入数字统计功能

6. **可视化结果展示**
   - 使用图表直观展示计算结果
   - 提供数据占比和详细数值分析

7. **单文件分发**
   - 打包为单一可执行文件，方便分发和使用
   - 无需安装Python或其他依赖环境

通过这个项目，我们展示了如何结合多种技术手段解决复杂的组合优化问题，并将高性能计算能力与友好的用户体验相结合。
